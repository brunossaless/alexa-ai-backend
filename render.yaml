# Blueprint Render — secrets só no painel (Environment), nunca no Git.
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: alexa-ai-backend
    runtime: docker
    region: oregon
    plan: free
    healthCheckPath: /health
